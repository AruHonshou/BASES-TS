(() => {
    'use strict';
    const fullName = ( firstName, lastName):string => {
        return `${firstName}, ${lastName}`;
    };
    
    const name = fullName( 'Tony', 'Stark');

    console.log( {name} );
    















})();